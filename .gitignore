# Python / Django
venv/
db.sqlite3
__pycache__/
*.pyc

# Rust build directory (the built WASM files in static/wasm/ ARE committed)
wasm/target/

# Self-signed certificates (the private key must not go into git)
certs/

# OS
.DS_Store
